Place real social media images in this folder and reference them from `data/sample_data.csv`.

The sample dataset points to files named `sample_001.jpg`, `sample_002.jpg`, and so on. If those files are missing, `python src/train.py` creates deterministic placeholder images so the feature pipeline can still run end to end.
